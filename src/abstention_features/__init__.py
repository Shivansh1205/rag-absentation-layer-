"""Feature extraction for the RAG abstention classifier (Phase 2).

Turns each Phase 1 (question, retrieved_chunks, label) row into a
fixed-size numeric feature vector. See `pipeline.extract_features` for
the single entry point, and README.md for the full feature table.
"""
